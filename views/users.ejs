<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Users</title>
</head>
<body class="bg-slate-50">
    <header class="flex max-md:flex-col max-md:space-y-5 justify-between items-center border-b-2 p-5 border-white bg-picton-blue">
        <h1 class="text-4xl text-center text-white font-bold">Users Manager App</h1>
        <a href="/logout" class="text-white hover:underline underline-offset-2 font-bold">Déconnexion</a>
    </header>
    <div class="flex flex-col justify-center inputs w-fit m-auto mt-16 space-y-5 pb-4">
        <h1 class="text-4xl text-center mt-8">Utilisateurs</h1>
        <% users.forEach(user => { %>
            <a class="block max-w-sm p-6 bg-picton-blue border border-gray-200 rounded-lg shadow hover:bg-gray-200 text-white hover:text-picton-blue">
                <h5 class="mb-2 text-2xl font-bold tracking-tight">User ID : <%= user.id %></h5>
                <p class="font-normal">Email : <%= user.email %></p>
            </a>
        <% }) %>
    </div>
</body>
</html>